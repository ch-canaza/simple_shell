#include "header.h"
/**
 * _strlen - Entry point
 * @s: pointer
 * Description
 * Return: 1 - Always success
 */

int _strlen(char *s)
{
        int i;
        int a;

        for (i = 0; s[a] != '\0'; i++)
        {
                a = i;
        }
        return (a);
}

